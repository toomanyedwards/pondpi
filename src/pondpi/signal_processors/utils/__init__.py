"""Generic building blocks used by some signal processors, not signal
processors themselves -- excluded from dynamic discovery because this
subpackage's name doesn't end in "_processor" (see
pondpi.signal_processors.discover_signal_processor_types).
"""
