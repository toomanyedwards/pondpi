"""Generic building blocks used by some signals, not signals themselves
-- excluded from dynamic discovery because this subpackage's name
doesn't end in "_signal" (see pondpi.signals.discover_signal_types).
"""
